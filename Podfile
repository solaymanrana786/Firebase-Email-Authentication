# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Email Authentication Firebase' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Email Authentication Firebase
  pod 'Firebase'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'

  target 'Email Authentication FirebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Email Authentication FirebaseUITests' do
    # Pods for testing
    
  
  end

end
